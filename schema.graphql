type Query {
    text(textCondition: TextCondition!): Text
    aaa: String
    iii: String
}

type Text {
    textId: ID! # テキストID
    textData: String! # テキスト内容
    hoge: String
    huga: String
}

input TextCondition {
    textId: ID! # テキストID
}